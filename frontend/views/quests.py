import streamlit as st

def page_quests():
    st.title("📜 クエスト一覧")
    
    # ユーザーがどのグループを見ているか選択させるUIなどが将来ここに入ります
    st.info("🚧 工事中：ここでクエストの確認や報告ができます")

    # --- タブのイメージだけ用意 ---
    tab1, tab2, tab3 = st.tabs(["未達成クエスト", "確認待ち", "完了履歴"])
    
    with tab1:
        st.write("現在挑戦できるクエストはありません")
    
    with tab2:
        st.write("承認待ちの報告はありません")
        
    with tab3:
        st.write("まだ履歴はありません")

    st.divider()
    # 戻るボタン
    if st.button("🏠 ホームに戻る"):
        st.session_state.current_page = "home"
        st.rerun()